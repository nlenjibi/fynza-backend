<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Fynza Backend Checkstyle configuration.
  Enforces rules the codebase actually follows; avoids fighting
  legitimate Spring Boot / Lombok / inline-override style.
-->
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <module name="LineLength">
        <property name="max" value="150"/>
        <property name="ignorePattern"
                  value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
    </module>

    <module name="NewlineAtEndOfFile"/>

    <module name="TreeWalker">

        <!-- Imports: only flag unused — star imports are codebase convention -->
        <module name="IllegalImport"/>
        <module name="UnusedImports"/>

        <!-- Coding: catch real bugs -->
        <module name="EmptyStatement"/>
        <module name="EqualsHashCode"/>
        <module name="IllegalInstantiation"/>
        <module name="StringLiteralEquality"/>
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>

        <!-- Misc -->
        <module name="UpperEll"/>
        <module name="OuterTypeFilename"/>

    </module>
</module>
