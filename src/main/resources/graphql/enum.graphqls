# REMOVED duplicate enum SortDirection to avoid redefinition error (now only in shared/enum.graphqls)

# Enum for sorting direction
enum SortDirection {
    ASC
    DESC
}

enum DeliveryMethod {
    DIRECT_ADDRESS
    BUS_STATION
    SHIPPING
}
