# Root types — defined once here. All module schemas use 'extend type Query' / 'extend type Mutation'.
# The schema {} block is intentionally omitted; graphql-java resolves Query/Mutation by naming convention.

type Query

type Mutation
